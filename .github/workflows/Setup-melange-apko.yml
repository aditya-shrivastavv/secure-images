name: Setup melange and apko
on:
  workflow_dispatch:
jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
    - name: Setup Go environment
      uses: actions/setup-go@v5.5.0
      with:
        go-version: 1.24.4
      
